package Unicode_IO.Linux is

   procedure Put
     (Text : String);

   procedure Put_Line
     (Text : String);

   procedure New_Line;

   procedure Put_Error
     (Text : String);

   procedure Put_Error_Line
     (Text : String);

end Unicode_IO.Linux;
